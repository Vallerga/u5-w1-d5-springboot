package com.prenotazione.configuration;

import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.springframework.context.annotation.Scope;

import com.prenotazioni.model.Edificio;
import com.prenotazioni.model.Postazione;
import com.prenotazioni.model.Tipo;

@Configuration
public class Prenotazioni_Configuration {
	@Bean
	@Scope("prototype")
	public Postazione postazioneconfig(String descrizionePostazione, Tipo tipo, Integer numMaxOccupanti, Edificio edificio) {
		return new Postazione(descrizionePostazione, tipo, numMaxOccupanti, edificio);
	}
}
